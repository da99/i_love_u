// Generated by CoffeeScript 1.3.3
(function() {
  var Procedure, arr_surgeon, as_num, englishy, funcy_perm, i_love_u, md_num, rw, string_da99, word_is_word, _,
    __slice = [].slice;

  englishy = require('englishy');

  string_da99 = require('string_da99');

  funcy_perm = require('funcy_perm');

  arr_surgeon = require('array_surgeon');

  _ = require('underscore');

  rw = require('rw_ize');

  Procedure = require("i_love_u/lib/Procedure");

  if (!RegExp.escape) {
    RegExp.escape = function(s) {
      return s.replace(/[-/\\^$*+?.()|[\]{}]/g, '\\$&');
    };
  }

  if (!RegExp.captures) {
    RegExp.captures = function(r, str) {
      var match, runs, vals;
      r.lastIndex = 0;
      match = null;
      vals = null;
      runs = 0;
      while ((match = r.exec(str))) {
        if (runs > 10 && r.lastIndex === 0) {
          throw new Error("/g flag is not set: " + r);
        }
        if (vals == null) {
          vals = [];
        }
        vals.push(match);
        runs += 1;
      }
      return vals;
    };
  }

  if (!RegExp.first_capture) {
    RegExp.first_capture = function(r, str) {
      var match, vals;
      r.lastIndex = 0;
      match = null;
      vals = null;
      return r.exec(str);
    };
  }

  exports.i_love_u = i_love_u = (function() {

    i_love_u.No_Match = "no_match";

    i_love_u.Base_Procs = [];

    rw.ize(i_love_u);

    i_love_u.read_write_able('address', 'pattern', 'data', 'procs', 'data');

    i_love_u.read_able('code', 'original_code');

    i_love_u.add_base_proc = function(proc) {
      this.Base_Procs.push(proc);
      return this.Base_Procs = this.Base_Procs.sort(function(a, b) {
        var a_level, b_level, levels;
        levels = {
          low: 10,
          medium: 0,
          high: -10
        };
        a_level = levels[a.priority()];
        b_level = levels[b.priority()];
        return a_level > b_level;
      });
    };

    function i_love_u(str) {
      this.rw_data().original_code = str;
      this.rw_data().code = str.standardize();
      this.write('procs', [].concat(this.constructor.Base_Procs));
      this._data_ = [];
    }

    i_love_u.prototype.add_to_data = function(k, v) {
      var obj;
      obj = {
        name: k,
        value: v,
        inherits_from: []
      };
      return this._data_.push(obj);
    };

    i_love_u.prototype.add_to_list = function(val) {
      return this._data_.push(val);
    };

    i_love_u.dynamic_data = function() {
      var args, func, k, name, v, _ref, _ref1;
      args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
      if (args.length === 0) {
        return this._dynamic_data_;
      } else if (args.length === 1) {
        name = args[0];
        _ref = this.constructor._dynamic_data_;
        for (k in _ref) {
          v = _ref[k];
          if (name === k || k.test(name)) {
            func = v;
          }
        }
        return func;
      } else if (args.length === 2) {
        if ((_ref1 = this._dynamic_data_) == null) {
          this._dynamic_data_ = [];
        }
        return this._dynamic_data_.push(args[0], args[1]);
      } else {
        throw new Error("Unknown args: " + args);
      }
    };

    i_love_u.dynamic_data(/^Block_Text_Line_[0-9]+$/, function(env, line, block, name) {
      var num, val;
      if (!block) {
        throw new Error("Block is not defined.");
      }
      num = parseInt(name.split('_').pop());
      val = block.text_line(num);
      return new Var(name, val);
    });

    i_love_u.prototype.dynamic_data = function(line, block, name) {
      var func;
      func = this.constructor.dynamic_data(name)[1];
      return func(this, line, block, name);
    };

    i_love_u.is_name_of_dynamic_data = function(name) {
      return !!this.dynamic_data(name);
    };

    i_love_u.prototype.is_name_of_data = function(k) {
      var v, val, _i, _len, _ref;
      if (this.constructor.is_name_of_dynamic_data(k)) {
        return true;
      }
      _ref = this._data_;
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        v = _ref[_i];
        if (v.name === k) {
          val = v;
        }
      }
      return !!val;
    };

    i_love_u.prototype.data = function(k) {
      var v, val, vals, _i, _len, _ref;
      if (k) {
        _ref = this._data_;
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          v = _ref[_i];
          if (v.name === k) {
            val = v;
          }
        }
        return val.value;
      } else {
        vals = (function() {
          var _j, _len1, _ref1, _results;
          _ref1 = this._data_;
          _results = [];
          for (_j = 0, _len1 = _ref1.length; _j < _len1; _j++) {
            v = _ref1[_j];
            if (("name" in v) && ("value" in v)) {
              _results.push(v);
            }
          }
          return _results;
        }).call(this);
        return vals;
      }
    };

    i_love_u.prototype.run = function() {
      var code_block, compiled, current, i, line, line_and_block, lines, matched, matched_to_proc, me, proc, _i, _j, _len, _len1, _ref, _ref1;
      lines = (new englishy.Englishy(this.code())).to_array();
      me = this;
      if ((_ref = this.compile_sentence_func) == null) {
        this.compile_sentence_func = function(memo, proc) {
          return proc.run(me, memo);
        };
      }
      for (i = _i = 0, _len = lines.length; _i < _len; i = ++_i) {
        line_and_block = lines[i];
        line = line_and_block[0];
        code_block = line_and_block[1];
        if (line && !code_block) {
          line = line.remove_end('.');
        } else if (line && code_block) {
          line = line.remove_end(':');
        }
        line = line.whitespace_split();
        current = [line, code_block];
        matched = true;
        while (matched) {
          compiled = current;
          _ref1 = this.procs();
          for (_j = 0, _len1 = _ref1.length; _j < _len1; _j++) {
            proc = _ref1[_j];
            matched_to_proc = true;
            while (matched_to_proc) {
              compiled = this.compile_sentence_func(current, proc);
              matched_to_proc = !_.isEqual(compiled, current);
              current = compiled;
            }
          }
          matched = !_.isEqual(compiled, current);
        }
      }
      return this.data();
    };

    return i_love_u;

  })();

  md_num = new Procedure("!>NUM< !>CHAR< !>NUM<");

  md_num.write('priority', 'high');

  md_num.write('procedure', function(match) {
    var m, n, op;
    m = match.args()[0];
    op = match.args()[1];
    n = match.args()[2];
    switch (op) {
      case '*':
        match.replace(parseFloat(m) * parseFloat(n));
        break;
      case '/':
        match.replace(parseFloat(m) / parseFloat(n));
        break;
      default:
        match.is_a_match(false);
    }
    return match;
  });

  as_num = new Procedure("!>NUM< !>CHAR< !>NUM<");

  as_num.write('procedure', function(match) {
    var m, n, op;
    m = match.args()[0];
    op = match.args()[1];
    n = match.args()[2];
    switch (op) {
      case '+':
        match.replace(parseFloat(m) + parseFloat(n));
        break;
      case '-':
        match.replace(parseFloat(m) - parseFloat(n));
        break;
      default:
        match.is_a_match(false);
    }
    return match;
  });

  word_is_word = new Procedure("!>WORD< is: !>ANY<.");

  word_is_word.write('procedure', function(match) {
    var name, val;
    name = _.first(match.args());
    val = _.last(match.args());
    match.env().add_to_data(name, val);
    match.replace(val);
    return match;
  });

  i_love_u.add_base_proc(as_num);

  i_love_u.add_base_proc(md_num);

  i_love_u.add_base_proc(word_is_word);

}).call(this);
